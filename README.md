# m3dscad

This is a 3D CAD language inspired by OpenSCAD, built on top of [model3d](github.com/unixpickle/model3d).

# Tests

To test against OpenSCAD, generate the reference STL files by running

```
./scripts/compile_openscad_testdata.sh
```
